name: mca_app
description: "AI-Powered Smart Food Rescue & Distribution System"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State Management
  provider: ^6.1.1
  
  # Real-time
  socket_io_client: ^3.0.2

  # Network & API
  dio: ^5.7.0
  # Utils
  intl: ^0.19.0
  timeago: ^3.7.0
  uuid: ^4.5.3
  path: ^1.9.0
  shared_preferences: ^2.3.2
  logger: ^2.5.0
  permission_handler: ^11.3.1
  
  # Images & Files
  image_picker: ^1.1.2
  cached_network_image: ^3.4.1
  url_launcher: ^6.3.1
  pdf: ^3.10.10
  printing: ^5.13.0
  open_file: ^3.3.1

  # Location & Maps
  geolocator: ^13.0.1
  geocoding: ^3.0.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  
  # Notifications
  flutter_local_notifications: ^17.2.3

  # Security
  local_auth: ^2.3.0
  flutter_pw_validator: ^1.6.0
  flutter_secure_storage: ^9.2.2

  # UI & Animation
  google_fonts: ^6.2.1
  shimmer: ^3.0.0
  flutter_spinkit: ^5.2.1
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.2.1
  flutter_svg: ^2.0.10+1
  signature: ^6.3.0
  syncfusion_flutter_charts: ^27.2.2
  flutter_animate: ^4.5.2
  path_provider: ^2.1.6
  device_info_plus: ^10.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
